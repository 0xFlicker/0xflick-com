mutation submitPublicResolver(
  $domain: ID!
  $addressEth: String!
  $textRecordEmail: String
) {
  createOrUpdateNameflick(
    domain: $domain
    fields: {
      addresses: { eth: $addressEth }
      textRecord: { email: $textRecordEmail }
    }
  ) {
    etherscan
  }
}
